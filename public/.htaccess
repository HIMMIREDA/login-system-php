<IfModule mod_rewrite.c>
    Options -Multiviews
    RewriteEngine on
    RewriteBase /login_system/public
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^.*$ register.php [l,R=301]
</IfModule>